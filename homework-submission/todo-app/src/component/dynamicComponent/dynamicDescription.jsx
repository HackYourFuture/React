import React from 'react';

const DynamicDescription = ({ description }) => <div>description: {description}</div>;
//  this line for render the description (dynamic)

export default DynamicDescription;
