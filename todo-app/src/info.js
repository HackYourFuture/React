import React from 'react';

export default class info extends React.Component {
    render() {
        return (
            <div>
                {this.props.text}
            </div>
        )
    }
}