{
  "name": "server",
  "version": "1.0.0",
  "description": "Connection for React and Express",
  "main": "server.js",
  "scripts": {
    "build": "cd ../react-hyf-client && npm run build",
    "server": "nodemon server.js",
    "start": "concurrently npm:build npm:server",
    "install-client": "cd ../react-hyf-client && npm install"
  },
  "author": "Alusine Q. Sesay",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.2",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^1.19.2"
  }
}
